# 使用文档

## 1. 数据库文件在项目根目录下
    sql文件已导出，不用执行数据迁移了

    fast-laravel-backend.sql

## 2. 后端页面端口和api的端口应该怎么修改
    api端口env里面修改


## 3. 超级用户账号
    email：superadmin@example.com  / admin@admin.com
    pssword：password

## 4. 启动
    php artisan serve --port=9000
    npm run dev

## 5. 报错
    

```

// 1. Illuminate\Encryption\MissingAppKeyException
    解决方案：php artisan key:generate

    数据迁移报错，删除2026_03_04_090008_add_reply_to_id_column_to_comment_table文件，迁移之后，
    
    // 执行下面的 添加字段并创建索引
        ALTER TABLE `comments`  ADD `reply_to_id` BIGINT UNSIGNED NULL DEFAULT NULL AFTER `parent_id`;
        -- 创建索引（提高查询效率）
        CREATE INDEX `comments_reply_to_id_index` ON `comments`(`reply_to_id`);


//2. 如果出现了 类似于Cannot declare class AddReplyToIdColumnToCommentTable, because the name is already in use
    // 删掉 class XXXXX，直接 return new class
    return new class extends Migration 
    {
        // 内部代码保持不变
        public function up() { ... }
        public function down() { ... }
    };
```

## 6. 登录
    http://localhost:9000/admin


